// Package middleware contains Blue's standard application middleware.
package middleware

import bluehttp "github.com/yasserelgammal/blue-go/v2/http"

// Middleware is an alias for the framework middleware function type.
type Middleware = bluehttp.Middleware
